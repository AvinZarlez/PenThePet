# js/config/

Application constants and derived configuration.
`constants.js` is the single source of truth for all numeric and string constants.
`config.js` contains derived values that reference `constants.js`.

See **[../../docs/CODE_STRUCTURE.md](../../docs/CODE_STRUCTURE.md)** for details.
